#include <bits/stdc++.h>
using namespace std;

int main()
{
    while(1)
    {
        int a, b;
        cin >> a >> b;
        int t = a+b;
        if(t == 0)
            break;
        cout << t << '\n';
    }
    
    
    return 0;
}