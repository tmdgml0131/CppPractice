#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a;
    cin >> a;
    while(a--)
    {
        int tmp;
        cin >> tmp;
        while(tmp--)
        {
            cout << "=";
        }
        cout << '\n';
    }
    
    return 0;
}